#echo client (создаем второй проект в новом окне)
import socket
from threading import Thread
HOST = '127.0.0.1'
PORT = 65432
server = HOST,PORT
def row():
    while True:
        data = s.recv(1024)
        print(data.decode('utf-8'))
print('Россия: Москва,Казань,Пермь. Мексика: Мехико, Тихуана. Италия: Рим, Милан, Неаполь')
with socket.socket(socket.AF_INET,socket.SOCK_DGRAM) as s:
    s.bind(('', 0))
    s.sendto(('/start').encode('utf-8'),server)
    r = Thread(target=row)
    r.start()
    while True:
        message=input()
        s.sendto((message).encode('utf-8'),server)
